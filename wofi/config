# Display Mode (choices: dmenu, run)
mode = "drun"

# Width and Height (in percentage of screen)
width = 600
height = 400

# Terminal emulator (for opening CLI apps)
term = "alacritty"

# Number of results per row
columns = 1

# Show Icons (if you have icon theme installed)
show_icons = true

# Match fuzzy text search
allow_markup = true
insensitive = true
